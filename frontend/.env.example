# Cloudflare Pages / Vite build-time API URL
# Local development can leave this unset because vite.config.js proxies /api to localhost:8000.
# Production should point to the public Render FastAPI URL, for example:
# VITE_API_BASE_URL=https://apollo-api.onrender.com
VITE_API_BASE_URL=
